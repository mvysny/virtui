# frozen_string_literal: true

# The tuile-based presentation layer: {UI::AppLayout} orchestrating the
# {UI::VMWindow} and {UI::SystemWindow}, the {UI::Formatter} helpers and the
# {UI::Theme} color tokens.
module UI
end
